<UserControl x:Class="PulseMeter.Slices.DailyUsage.UI.DailyUsageSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border x:Name="DailyUsagePanel"
                                    Margin="0,12,0,0"
                                    Padding="16"
                                    Style="{DynamicResource LightCardStyle}"
                                    Visibility="{Binding DataContext.IsDailyUsageVisible, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <Grid>
                                        <StackPanel>
                                            <Button Padding="0"
                                                    HorizontalAlignment="Left"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    ToolTip="{Binding DailyUsageExpandCollapseTooltip}"
                                                    Click="DailyUsageExpandCollapseButton_Click">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <ContentPresenter />
                                                    </ControlTemplate>
                                                </Button.Template>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="DAILY USAGE"
                                                               Foreground="#111827"
                                                               FontSize="20"
                                                               FontWeight="Bold" />
                                                    <Border x:Name="DailyUsageEvidenceLabel"
                                                            Style="{DynamicResource LightEvidencePillStyle}">
                                                        <TextBlock Text="{Binding DataContext.SyncStatusText, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                   Style="{DynamicResource LightEvidencePillTextStyle}" />
                                                    </Border>
                                                </StackPanel>
                                            </Button>
                                             <TextBlock x:Name="MedianComparisonText"
                                                       Margin="0,8,0,0"
                                                       Text="{Binding DailyUsageMedianSummaryText}"
                                                       Foreground="#4B5563"
                                                       FontSize="14"
                                                       Visibility="{Binding HasDailyUsageMedianSummary, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        </StackPanel>

                                        <Button Padding="14,8"
                                                HorizontalAlignment="Right"
                                                Background="#FFFFFF"
                                                BorderBrush="#E5E7EB"
                                                BorderThickness="1"
                                                ToolTip="{Binding DailyUsageExpandCollapseTooltip}"
                                                Click="DailyUsageExpandCollapseButton_Click">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="6"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center" />
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                            <StackPanel Orientation="Horizontal"
                                                        VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DailyUsageWindowText}"
                                                           Foreground="#111827"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           VerticalAlignment="Center" />
                                                <Path Width="10"
                                                      Height="6"
                                                      Margin="9,1,0,0"
                                                      Stroke="#4B5563"
                                                      StrokeThickness="1.4"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      StrokeLineJoin="Round"
                                                      Stretch="Fill"
                                                      VerticalAlignment="Center">
                                                    <Path.Style>
                                                        <Style TargetType="Path">
                                                            <Setter Property="Data" Value="M 0 0 L 5 6 L 10 0" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsDailyUsageExpanded}" Value="True">
                                                                    <Setter Property="Data" Value="M 0 6 L 5 0 L 10 6" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Path.Style>
                                                </Path>
                                            </StackPanel>
                                        </Button>
                                    </Grid>

                                    <StackPanel x:Name="DailyUsagePanelBody"
                                                Margin="0,12,0,0"
                                                Visibility="{Binding IsDailyUsageExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="149" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="118" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="Day"
                                                       Foreground="#374151"
                                                       FontSize="14" />
                                            <TextBlock Grid.Column="3"
                                                       Text="Tokens"
                                                       Foreground="#374151"
                                                       FontSize="14"
                                                       HorizontalAlignment="Left" />
                                        </Grid>
                                        <Border Height="1"
                                                Margin="0,13,0,0"
                                                Background="#E5E7EB" />

                                        <ItemsControl ItemsSource="{Binding DailyUsageRows}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid MinHeight="34">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="149" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="24" />
                                                            <ColumnDefinition Width="118" />
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="1" />
                                                        </Grid.RowDefinitions>

                                                        <StackPanel Orientation="Horizontal"
                                                                    VerticalAlignment="Center">
                                                            <Ellipse x:Name="DailyUsageDayDot"
                                                                     Width="7"
                                                                     Height="7"
                                                                     Margin="3,0,17,0"
                                                                     Fill="#1F73FF" />
                                                            <TextBlock Text="{Binding DateText}"
                                                                       Foreground="#111827"
                                                                       FontSize="14"
                                                                       VerticalAlignment="Center" />
                                                        </StackPanel>

                                                        <ProgressBar Grid.Column="1"
                                                                     Height="6"
                                                                     HorizontalAlignment="Stretch"
                                                                     Minimum="0"
                                                                     Maximum="100"
                                                                     Value="{Binding BarPercentValue, Mode=OneWay}"
                                                                     Style="{DynamicResource LightProgressBarStyle}"
                                                                     VerticalAlignment="Center" />

                                                        <StackPanel Grid.Column="3"
                                                                    HorizontalAlignment="Left"
                                                                    VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding TokenText}"
                                                                       Foreground="#111827"
                                                                       FontSize="14"
                                                                       FontWeight="SemiBold" />
                                                            <TextBlock Text="{Binding MedianComparisonText}"
                                                                       Foreground="#4B5563"
                                                                       FontSize="12"
                                                                       Visibility="{Binding HasMedianComparison, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                        </StackPanel>

                                                        <Border Grid.Row="1"
                                                                Grid.ColumnSpan="4"
                                                                Background="#E5E7EB" />
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                    </StackPanel>
                                </StackPanel>
                            </Border>
</UserControl>
