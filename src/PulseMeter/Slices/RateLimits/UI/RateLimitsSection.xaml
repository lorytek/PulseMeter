<UserControl x:Class="PulseMeter.Slices.RateLimits.UI.RateLimitsSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border x:Name="RateLimitsPanel"
                                    Padding="16"
                                    Style="{DynamicResource LightCardStyle}"
                                    Visibility="{Binding DataContext.IsRateLimitsVisible, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="RATE LIMITS"
                                               Foreground="#111827"
                                               FontSize="20"
                                               FontWeight="Bold" />

                                    <Grid Margin="0,14,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="56" />
                                                <ColumnDefinition Width="136" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="Track"
                                                       Foreground="#374151"
                                                       FontSize="14"
                                                       VerticalAlignment="Center" />
                                            <ComboBox Grid.Column="1"
                                                      ItemsSource="{Binding LimitOptions}"
                                                      SelectedItem="{Binding SelectedLimitOption, Mode=TwoWay}"
                                                      DisplayMemberPath="DisplayName"
                                                      Style="{DynamicResource LightComboBoxStyle}" />
                                        </Grid>

                                        <ItemsControl Grid.Column="1"
                                                      Margin="34,-10,0,0"
                                                      ItemsSource="{Binding SelectedQuotaRows}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Width="184"
                                                                Margin="0,0,44,4"
                                                                HorizontalAlignment="Center">
                                                        <TextBlock Margin="0,0,0,6"
                                                                   Text="{Binding RingSubtitleText}"
                                                                   Foreground="#6B7280"
                                                                   FontSize="13"
                                                                   FontWeight="SemiBold"
                                                                   TextAlignment="Center"
                                                                   HorizontalAlignment="Center" />
                                                        <Viewbox Width="132"
                                                                 Height="132"
                                                                 HorizontalAlignment="Center">
                                                            <Grid Width="112"
                                                                  Height="112">
                                                                <Path Stroke="#E5E7EB"
                                                                      StrokeThickness="7">
                                                                    <Path.Data>
                                                                        <EllipseGeometry Center="56,56"
                                                                                         RadiusX="43"
                                                                                         RadiusY="43" />
                                                                    </Path.Data>
                                                                </Path>
                                                                <Path Data="{Binding RingArcData}"
                                                                      Stroke="{Binding RingBrush}"
                                                                      StrokeThickness="7"
                                                                      StrokeStartLineCap="Round"
                                                                      StrokeEndLineCap="Round"
                                                                      StrokeLineJoin="Round" />
                                                                <StackPanel HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center">
                                                                    <TextBlock Text="{Binding RingPercentText}"
                                                                               Foreground="#111827"
                                                                               FontSize="28"
                                                                               FontWeight="Bold"
                                                                               TextAlignment="Center"
                                                                               HorizontalAlignment="Center" />
                                                                </StackPanel>
                                                            </Grid>
                                                        </Viewbox>
                                                        <TextBlock Margin="0,8,0,0"
                                                                   Text="{Binding ResetDisplayText}"
                                                                   Foreground="#6B7280"
                                                                   FontSize="13"
                                                                   FontWeight="SemiBold"
                                                                   TextAlignment="Center"
                                                                   HorizontalAlignment="Center" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <TextBlock Grid.Row="1"
                                                   Grid.ColumnSpan="2"
                                                   Margin="0,12,0,0"
                                                   Text="{Binding DataContext.StatusMessage, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   Foreground="#DC2626"
                                                   FontSize="12"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding DataContext.HasStatusMessage, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Grid>
                                </StackPanel>
                            </Border>
</UserControl>
